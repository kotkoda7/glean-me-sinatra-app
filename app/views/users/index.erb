<h1>Hello,
    <%= @user.username %>
</h1>

<% if !@user.edibles.empty? && !@user.locations.empty? %>
 <h2>
  
    <h3>This is a list of your entered locations:</h3>
   
    Click on a location ID to add any changes.
<br>
 <br>
<% @user.locations.each do |location| %>
<li>
    <a href="/locations/<%=location.id%>">
       #<%= location.id %> </a>, <%= location.address %> <%=location.edible.name%>
</li>
<% end %>

<% elsif @user.edibles.empty? && @user.locations.empty? %>
>Your location library is currently empty
<% end %>
<br>
>Add
    <a href="/locations/new">new locations</a>,

<li>
    
        <a href="/edibles">All edibles</a>
    </li>

<li>
    
        <a href="/locations">All locations</a>
    </li>

