<h2>Edit location
    #<%=@location.id%>:
</h2>

<h3>
<form action="/locations/<%= @location.id %>/edit" method="POST">


  
    <label>Change address to: </label>
    <input type="text" name="location[address]" value="<%=@location.address%>">
  <br>
    <label>Change latitude to: </label>
    <input type="number" name="location[lat]" value="<%=@location.lat%>">

  <br>
    <label>Change longitude to: </label>
    <input type="number" name="location[lng]" value="<%=@location.lng%>">
    
  <br>

  <label>Location type:</label>
<select name="location[loc_type]">
        <% @locations.each do |location| %>
          <option value=<%=@location.id%> <%= "selected" if location.id==@location.loc_type%> ><%=location.loc_type%> 
        </option>     
        <% end %>
    </select>
<br>


Change food available at this location:

  <ul>
     <li><select name="location[edible]"></li>
        <% @locations.each do |location| %>
          <option value=<%=@location.edible%> <%= "selected" if location.edible==@location.edible%> ><%=location.edible%> 
        </option>     
        <% end %>
    </select>
  </ul>


  <br>

    
    <input type="submit" value="Submit">
   
  </form>

