Add food to this location

<h2>Add a new food category to this location: </h2>
    <h3>
    	<ul>
    		<li><%=@location.address%></li>
    <li><%=@location.lat%></li>
    <li><%=@location.lng%></li>
		</ul>
	<h3

<form action="/locations/<%=@location.id%>/add_edible" method="post">

  <h3>
      <label>Choose a new food for this location:</label>
  </h3>

  <select name="selected_edible">
      <option value="">
        -
      </option>
      <% @edibles.each do |edible| %>
        <option value="<%=edible.name%>"><%=edible.name%></option>
      <% end %>
  </select>

      <input type="submit" value="Add new food type">
</form>
  <br>
<h3>
  If the food category you want to add doesn't exist please click here first:
    <a href="/edibles/new">add category</a><h3>
<br>